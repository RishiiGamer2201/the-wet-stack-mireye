# Moved to the repository root.
#
# Render only auto-detects a Blueprint named `render.yaml` at the root of the
# repository, so keeping the real one here made it invisible to Render. The
# canonical, deployable Blueprint is now:
#
#     ../render.yaml
#
# Setup steps and environment variables: ../docs/deployment.md
